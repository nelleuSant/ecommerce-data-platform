version: 2

models:
  - name: dim_clientes
    columns:
      - name: cliente_key
        tests:
          - unique
          - not_null

  - name: dim_veiculos
    columns:
      - name: veiculo_key
        tests:
          - unique
          - not_null

  - name: fact_vendas
    columns:
      - name: venda_key
        tests:
          - unique
          - not_null
      - name: cliente_key
        tests:
          - not_null
      - name: valor_pago
        tests:
          - not_null